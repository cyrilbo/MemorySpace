import React, { FunctionComponent } from 'react';
import styled from '../../core/theme/styled-components';

export const CreateCardModal: FunctionComponent = () => {
  return <Container />;
};

const Container = styled.SafeAreaView``;
